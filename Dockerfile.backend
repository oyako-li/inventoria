# 1. ベースイメージの選択
FROM python:3.9-slim

# 2. 作業ディレクトリの設定
WORKDIR /app

# 3. 必要なパッケージのインストール
RUN pip install uv

# 4. 依存関係ファイルをコピーしてインストール
COPY pyproject.toml uv.lock ./
RUN uv pip sync pyproject.toml --system

# 5. アプリケーションのコードをコピー
COPY . .

# 6. FastAPIサーバーの起動コマンド
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"] 